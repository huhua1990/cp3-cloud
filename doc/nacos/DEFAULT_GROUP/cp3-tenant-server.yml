cp3:
  database:
    isSeata: false
    tenantDatabasePrefix: cp3_base
  swagger:
    enabled: true
    docket:
      tenant:
        title: 租户模块
        base-package: com.cp3.cloud.tenant.controller

server:
  port: 8778
